spring:
  cloud:
    openfeign:
      ## TODO 요청 압축 설정, request나 response가 커질 것 같으면 true
      compression:
        request:
          enabled: false
          min-request-size: 2048
          mime-types: application/json
        response:
          enabled: false